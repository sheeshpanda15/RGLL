# ROG_aligned.R
# -----------------------------------------------------------------------------
# Backward-compatible entry point.
#
# The canonical implementation now lives in ROG_aligned_fixed.R.  Keep this file
# so older scripts that source("ROG_aligned.R") use the paper-aligned RASC/SGA
# implementation without triggering any automatic experiment.
# -----------------------------------------------------------------------------

source("ROG_aligned_fixed.R")
